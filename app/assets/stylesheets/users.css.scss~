// Place all the styles related to the Users controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

#loggedInUser {
  width: 50%;
  margin: 0 auto;
}

#loggedInUser td {
  padding-bottom: 20px;
}

#loggedInUser td:nth-child(even) {
  text-align:right;
}

.roomLinks {
  font-size:12pt;
}
